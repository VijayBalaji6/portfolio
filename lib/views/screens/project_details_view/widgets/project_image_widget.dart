import 'package:flutter/material.dart';

class ProjectDetailWidget extends StatelessWidget {
  const ProjectDetailWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
